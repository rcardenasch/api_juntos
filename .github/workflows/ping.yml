name: Wake Up Render

on:
  schedule:
    # cada 1000 minutos
    - cron: "*/1000 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render App
        run: |
          curl -L https://api-juntos.onrender.com/health
